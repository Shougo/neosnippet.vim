snippet func
alias   function
  function ${1:function_name}(${2:argument})
    ${0:// body...}
  end

snippet if
  if (${1:true}) then
    ${0:$TM_SELECTED_TEXT}
  end

snippet for
  for ${2:i} = 0, ${1:Things} do
    ${0}
  end

snippet forin
  for ${2:k}, ${3:v} in ${1:ipairs(xs)} do
    ${0}
  end
